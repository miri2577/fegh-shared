name: fegh_compliance
description: Gemeinsame Compliance-Services fuer FEGH-Apps. DSGVO-konformer Audit-Logger (JSON-Lines) und DSGVO-Export-Helper.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
